# 环境检查记录

## 基本信息

- 阶段：阶段 0 - 环境准备
- 检查日期：2026-06-08
- 项目目录：`C:\Users\佘晨铭\Desktop\学习路线\enterprise-admin-system`
- GitHub 仓库：`https://github.com/free-loop1/enterprise-admin-system.git`

## 环境检查结果

| 检查项 | 实际结果 | 状态 |
| --- | --- | --- |
| JDK | `25.0.3 LTS`，安装路径 `D:\Java\jdk-25` | 通过 |
| Java 程序运行 | 已通过 `jshell` 输出 `JAVA_PROGRAM_OK` | 通过 |
| IntelliJ IDEA | `D:\JETBRAINS\IntelliJ IDEA 2026.1.1` | 通过 |
| Maven | `Apache Maven 3.9.16`，安装路径 `D:\Maven\apache-maven-3.9.16` | 通过 |
| Git | `git version 2.39.1.windows.1` | 通过 |
| MySQL | `MySQL Community Server 9.7.0` | 通过 |
| MySQL 数据库 | 已创建 `enterprise_admin_system` | 通过 |
| Docker Desktop | `Docker version 29.5.2` | 通过 |
| Redis | 使用 Docker/WSL 环境启动，已验证 Redis 返回 `PONG` | 通过 |
| GitHub/Gitee 仓库 | 已创建 GitHub 仓库 `free-loop1/enterprise-admin-system` | 通过 |

说明：学习路线原任务写的是 JDK 21，本机实际使用的是更新的 JDK 25 LTS。若后续课程或项目强制要求 JDK 21，可将 `JAVA_HOME` 临时切换到 JDK 21。

## 验证命令记录

```powershell
java -version
javac -version
mvn -version
git --version
mysql --version
docker --version
```

关键验证结果：

```text
java version "25.0.3" 2026-04-21 LTS
Apache Maven 3.9.16
MySQL Server 9.7.0
Docker version 29.5.2
```

MySQL 验证：

```sql
SELECT VERSION();
CREATE DATABASE enterprise_admin_system DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
SHOW DATABASES;
```

验证结果：

```text
VERSION(): 9.7.0
enterprise_admin_system 数据库已创建
```

Redis 验证：

```powershell
docker exec redis-dev redis-cli ping
```

验证结果：

```text
PONG
```

## Git 配置

```text
user.name = free-loop1
user.email = 已配置
init.defaultBranch = main
```

## 项目结构

```text
enterprise-admin-system
├── deploy
├── docs
├── notes
├── sql
├── README.md
└── 环境检查记录.md
```

## 第一次 Git 提交

```text
提交编号：37f004e
提交说明：chore: initialize project structure
本地分支：main
远程分支：origin/main
远程仓库：https://github.com/free-loop1/enterprise-admin-system.git
```

常用 Git 命令：

```powershell
git status
git add .
git commit -m "提交说明"
git push
git pull
```

## 服务启动说明

MySQL 和 Docker Desktop 当前设置为手动启动，需要使用时可执行：

```powershell
net start MySQL97
```

Docker Desktop 建议从开始菜单打开。Redis 容器常用命令：

```powershell
docker start redis-dev
docker stop redis-dev
docker exec redis-dev redis-cli ping
```

## 阶段结论

阶段 0 环境准备已完成。环境安装完成，MySQL 数据库创建完成，GitHub 仓库推送完成，第一次 Git 提交完成。已具备 Java 后端开发所需的 JDK、IDEA、Maven、Git、MySQL、Redis、Docker Desktop 环境。
